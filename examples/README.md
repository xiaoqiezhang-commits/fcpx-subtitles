# Synthetic example

`segments.tsv` contains invented interview text and no client material.

From the repository root, run the generation and validation commands shown in the main [README](../README.md). Generated `.fcpxml` files are written to `output/`, which is excluded from Git.

The positions and typography in the command are illustrative. Confirm them for every real project before generating delivery files.
